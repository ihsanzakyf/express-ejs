# 🚀 Web App with Express.js, EJS, and Bootstrap

A simple web application built using **Express.js** as the backend framework, **EJS** as the templating engine, and **Bootstrap 5** for responsive UI design.

---